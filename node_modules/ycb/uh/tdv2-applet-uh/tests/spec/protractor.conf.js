var baseConfig = require('protractor-utils').mochaBaseConf;

baseConfig.suites = {
    'full': ['test/**/*.spec.js'],
    'react': ['test/**/react*.spec.js']
};

baseConfig.multiCapabilities = [{
    browserName: 'chrome',
    version: 'latest'
}];

baseConfig.mochaOpts = {
    reporter: 'spec',
    timeout: 30000
};

// Note that we are exporting a config object, so it's exports.config
exports.config = baseConfig;
