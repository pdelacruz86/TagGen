/*jslint indent:4, nomen:true, white:true, node:true */

'use strict';

var React = require('react/addons');

var I13nAnchor = React.createClass({
    getProps: function() {
        var props = this.props;
        return Object.keys(props).reduce(
            function (clonedProps, key) {
                clonedProps['data-' + key.toLowerCase()] = props[key];
                return clonedProps;
            }, {});
    },
    render: function() {
        return <div className='i13n-anchor' { ... this.getProps() } />;
    }
});

module.exports = {
    I13nAnchor: I13nAnchor
};
