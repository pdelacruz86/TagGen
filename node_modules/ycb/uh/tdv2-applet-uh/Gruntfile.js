'use strict';

module.exports = function (grunt) {
    grunt.loadTasks('./tasks');
    
    grunt.registerTask('default', ['applet_version', 'tdv2-applet-dev']);
    grunt.registerTask('dev', ['applet_version', 'tdv2-applet-dev']);

    grunt.registerTask('css', [
        'locator',
        'atomizer-merge-configs',
        'atomizer',
        'atomizer-context',
        'postcss'
    ]);

    grunt.registerTask('build', [
        'react',
        'locator',
        'loaderjs',
        'css',
        'hash_files',
        'webpack:client-min'
    ]);
};
