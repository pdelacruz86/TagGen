'use strict';

module.exports = []
    // master
    // .concat(require('../../components/master'))
    .concat(require('./master'))
    // delta
    .concat(require('./site/'))
    .concat(require('./theme/'))
    .concat(require('./partner/'))
    ;
