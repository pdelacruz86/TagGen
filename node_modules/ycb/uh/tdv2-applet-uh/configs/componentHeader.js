'use strict';

// var debug = require('debug')('uh:configs');

function ycb(config) {
    var data = [];

    Object.keys(config).forEach(function getConfig(name, i) {
        // debug(['ycb', 'i', i, 'name', name, require('util').inspect(config[name])].join(' '));

        data = data.concat(config[name]);
    });
require('fs').writeFileSync('./header.js', JSON.stringify(data, null, 4));
    return data;
}

module.exports = ycb(require('./components/'));
