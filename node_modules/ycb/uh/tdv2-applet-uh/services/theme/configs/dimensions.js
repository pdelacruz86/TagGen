var commonDimensions = require('common-dimensions');
var customDimensions = [
        {
            dimensions: [
                {
                    theme: {
                        dark: null,
                        'default': null,
                        mega: null
                    }
                }
            ]
        }
    ];
var lib = require('td-lib-common');
module.exports = lib.mergeDimensions(commonDimensions, customDimensions);
